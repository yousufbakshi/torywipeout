import React from "react";

const HexMap = ({ hexData }: { hexData: any }) => {
  return (
    <div className="hex-map">{/* Render your hex map using hexData */}</div>
  );
};

export default HexMap;
